# 安装与更新

```powershell
pip install ymcp
pip install -U ymcp
```

本地开发安装：

```powershell
python -m pip install -e .[dev]
```

安装后建议检查：

```powershell
ymcp doctor
ymcp --version
ymcp inspect-tools --json
```
